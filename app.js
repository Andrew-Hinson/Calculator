const screen = document.querySelector('#screen');






const add = (x, y) =>  x + y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;
const operate = (opp, x, y) => opp(x, y);

let displayValue = 0;
